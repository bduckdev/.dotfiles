#!/bin/bash

polybar-msg cmd quit

polybar primary 2>&1

if xrandr | grep "DP-1-2 connected"  2> /dev/null; then
    polybar-msg cmd restart
    nitrogen --restore
    polybar secondary 2>&1 
    polybar fakenews 2>&1 
fi
